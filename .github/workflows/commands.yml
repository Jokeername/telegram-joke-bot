name: Bot Commands

on:
  workflow_dispatch:
    inputs:
      command:
        description: 'Command to execute'
        required: true
        default: '/help'
        type: choice
        options:
        - /start
        - /help
        - /joke
        - /time
        - /stats

jobs:
  execute-command:
    runs-on: ubuntu-latest
    steps:
    - name: Process command
      run: |
        COMMAND="${{ github.event.inputs.command }}"
        BOT_TOKEN="8219894042:AAG1PJrwW22WpIvaW9pr69rLkQ4wTpa40Qg"
        CHAT_ID="1810598168"
        
        echo "🔧 Executing command: $COMMAND"
        
        case $COMMAND in
          "/start")
            MESSAGE="🤖 Привет! Я бот с шутками! Отправляю утренние шутки в 10:00 и вечерние анекдоты в 21:00 по Москве. Используй /help для списка команд"
            ;;
          "/help")
            MESSAGE="📋 Доступные команды:
/start - Приветствие
/help - Список команд  
/joke - Случайная шутка сейчас
/time - Время отправки
/stats - Статистика"
            ;;
          "/joke")
            JOKES=("🎭 Случайная шутка: Почему программисты путают Хэллоуин и Рождество? Oct 31 == Dec 25!" "🎭 Случайная шутка: Сколько программистов нужно для лампочки? Ни одного!" "🎭 Случайная шутка: Фитнес - это когда платишь деньги, чтобы болело тело!")
            MESSAGE=${JOKES[$RANDOM % ${#JOKES[@]}]}
            ;;
          "/time")
            MESSAGE="⏰ Расписание:
🌞 Утро: 10:00 по Москве
🌜 Вечер: 21:00 по Москве"
            ;;
          "/stats")
            MESSAGE="📊 Статистика:
• Шуток отправлено: 15
• Анекдотов отправлено: 12
• Работает с: 15 ноября"
            ;;
          *)
            MESSAGE="❌ Неизвестная команда. Используй /help"
            ;;
        esac
        
        echo "📨 Sending: $MESSAGE"
        curl -s -X POST "https://api.telegram.org/bot$BOT_TOKEN/sendMessage" \
          -d chat_id="$CHAT_ID" \
          -d text="$MESSAGE"
        
        echo "✅ Command executed successfully!"
